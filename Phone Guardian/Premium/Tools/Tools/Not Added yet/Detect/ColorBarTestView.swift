//
//  ColorBarTestView.swift
//  Phone Guardian
//
//  Created by Kevin Doyle Jr. on 11/26/24.
//
/*

import SwiftUI

struct ColorBarTestView: View {
    var colors: [Color] = [.red, .green, .blue, .yellow, .orange, .purple, .white, .black]

    var body: some View {
        VStack(spacing: 0) {
            ForEach(colors, id: \.self) { color in
                color
                    .frame(height: UIScreen.main.bounds.height / CGFloat(colors.count))
            }
        }
        .ignoresSafeArea()
        .navigationTitle("Color Bar Test")
    }
}
*/
