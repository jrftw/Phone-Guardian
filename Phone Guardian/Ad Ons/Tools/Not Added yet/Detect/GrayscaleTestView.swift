//
//  GrayscaleTestView.swift
//  Phone Guardian
//
//  Created by Kevin Doyle Jr. on 11/26/24.
//
/*

import SwiftUI

struct GrayscaleTestView: View {
    var body: some View {
        VStack(spacing: 0) {
            ForEach(0..<10) { i in
                Rectangle()
                    .fill(Color(white: Double(i) / 9.0))
                    .frame(height: UIScreen.main.bounds.height / 10)
            }
        }
        .ignoresSafeArea()
        .navigationTitle("Grayscale Test")
    }
}
*/
