/* import SwiftUI

struct DetectView: View {
    var body: some View {
        List {
            Section(header: Text("Detect Tools")) {
                NavigationLink(destination: LCDScreenTestView()) {
                    HStack {
                        Image(systemName: "rectangle.on.rectangle")
                        Text("LCD Screen Test")
                    }
                }

                NavigationLink(destination: MultiTouchTestView()) {
                    HStack {
                        Image(systemName: "hand.raised.fill")
                        Text("Multi-Touch Test")
                    }
                }

                NavigationLink(destination: ColorBarTestView()) {
                    HStack {
                        Image(systemName: "paintpalette.fill")
                        Text("Color Bar Test")
                    }
                }

                NavigationLink(destination: GrayscaleTestView()) {
                    HStack {
                        Image(systemName: "circle.lefthalf.filled")
                        Text("Grayscale Test")
                    }
                }

                NavigationLink(destination: ControllableAreaTestView()) {
                    HStack {
                        Image(systemName: "square.split.2x2.fill")
                        Text("Controllable Area Test")
                    }
                }
            }
        }
        .navigationTitle("Detect Tools")
    }
}
 */
